pipeline {
    agent any

    stages {
        stage('Code Checkout') {
            steps {
                // הפקודה הזו אומרת לג'נקינס למשוך את הקוד מה-Repository הנוכחי
                checkout scm
            }
        }
    }
}
